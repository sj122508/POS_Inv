import React from "react";
// import { connect } from 'react-redux'

function POS() {
	return <div>HELLO IM HERE POS</div>;
}

export default POS;

// const mapStateToProps = (state) => ({})

// const mapDispatchToProps = {}

// export default connect(mapStateToProps, mapDispatchToProps)(POS)
